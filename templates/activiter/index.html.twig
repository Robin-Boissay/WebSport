{% extends 'base.html.twig' %}

{% block title %}Hello ActiviterController!{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/activiter.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-4">📋 Liste des Activités</h1>
        <div class="wrapperActiviter">


        {% for activiter in activiters %}
            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <h3 class="card-title text-primary">🏆 {{ activiter.nomActiviter }}</h3>
                    <h5 class="text-secondary">🔹 Type : {{ activiter.typeActiviter }}</h5>

                    {% if activiter.donnees is not empty %}
                        <table class="table mt-3">
                            <thead class="thead-dark">
                            <tr>
                                <th>Type de Donnée</th>
                                <th>Valeur</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for donnee in activiter.donnees %}
                                <tr>
                                    <td>{{ donnee.ProprieterActiviter }}</td>
                                    <td><strong>{{ donnee.valeur }}</strong></td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    {% else %}
                        <p class="text-muted">Aucune donnée enregistrée.</p>
                    {% endif %}
                </div>
            </div>
        {% else %}
            <p class="text-center text-danger">Aucune activité enregistrée.</p>
        {% endfor %}
        </div>
    </div>
{% endblock %}